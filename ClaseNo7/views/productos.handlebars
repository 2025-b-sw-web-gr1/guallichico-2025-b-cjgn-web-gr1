<style>
    .page-header {
        background: linear-gradient(135deg, rgba(255, 0, 128, 0.2), rgba(0, 240, 255, 0.2));
        padding: 2rem;
        border-radius: 15px;
        margin-bottom: 2rem;
        border: 2px solid var(--primary);
    }

    .page-header h1 {
        font-size: 2.5rem;
        background: linear-gradient(135deg, var(--primary), var(--accent));
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        margin-bottom: 0.5rem;
    }

    .page-header p {
        color: var(--text-muted);
        font-size: 1.1rem;
    }

    .filtros-container {
        display: flex;
        flex-wrap: wrap;
        gap: 1rem;
        margin-bottom: 2rem;
        padding: 1.5rem;
        background: rgba(26, 31, 58, 0.6);
        border-radius: 15px;
        border: 2px solid var(--primary);
    }

    .filtro-btn {
        padding: 0.7rem 1.5rem;
        background: rgba(255, 0, 128, 0.1);
        border: 2px solid var(--primary);
        border-radius: 25px;
        color: var(--text);
        text-decoration: none;
        transition: all 0.3s;
        font-weight: 500;
    }

    .filtro-btn:hover,
    .filtro-btn.active {
        background: linear-gradient(135deg, var(--primary), var(--secondary));
        border-color: var(--primary);
        transform: scale(1.05);
        box-shadow: 0 5px 20px rgba(255, 0, 128, 0.4);
    }

    .productos-info {
        text-align: center;
        margin-bottom: 2rem;
        color: var(--text-muted);
        font-size: 1.1rem;
    }

    .productos-grid {
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
        gap: 2rem;
    }

    .producto-card {
        background: rgba(26, 31, 58, 0.6);
        border-radius: 15px;
        overflow: hidden;
        border: 2px solid transparent;
        transition: all 0.3s;
        position: relative;
    }

    .producto-card:hover {
        border-color: var(--primary);
        transform: translateY(-10px);
        box-shadow: 0 15px 40px rgba(255, 0, 128, 0.4);
    }

    .producto-badge {
        position: absolute;
        top: 1rem;
        right: 1rem;
        background: linear-gradient(135deg, var(--primary), var(--secondary));
        color: white;
        padding: 0.5rem 1rem;
        border-radius: 20px;
        font-size: 0.9rem;
        font-weight: bold;
        z-index: 10;
    }

    .producto-imagen {
        width: 100%;
        height: 220px;
        background: linear-gradient(135deg, var(--primary), var(--secondary));
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 4.5rem;
        color: white;
    }

    .producto-contenido {
        padding: 1.5rem;
    }

    .producto-categoria {
        display: inline-block;
        background: rgba(0, 240, 255, 0.2);
        color: var(--accent);
        padding: 0.3rem 0.8rem;
        border-radius: 15px;
        font-size: 0.85rem;
        margin-bottom: 0.5rem;
        border: 1px solid var(--accent);
        text-transform: uppercase;
    }

    .producto-nombre {
        font-size: 1.2rem;
        margin-bottom: 0.5rem;
        color: var(--text);
        font-weight: bold;
    }

    .producto-rating {
        color: var(--warning);
        margin-bottom: 1rem;
        font-size: 1rem;
    }

    .producto-descripcion {
        color: var(--text-muted);
        margin-bottom: 1rem;
        font-size: 0.9rem;
        line-height: 1.5;
        display: -webkit-box;
        -webkit-line-clamp: 2;
        -webkit-box-orient: vertical;
        overflow: hidden;
    }

    .producto-precio {
        margin-bottom: 1rem;
    }

    .precio-original {
        text-decoration: line-through;
        color: var(--text-muted);
        font-size: 0.95rem;
    }

    .precio-actual {
        font-size: 1.8rem;
        font-weight: bold;
        background: linear-gradient(135deg, var(--primary), var(--accent));
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
    }

    .producto-stock {
        margin-bottom: 1rem;
        padding: 0.5rem;
        background: rgba(0, 255, 136, 0.1);
        border-radius: 8px;
        text-align: center;
        color: var(--success);
        font-size: 0.85rem;
    }

    .btn-ver {
        width: 100%;
        padding: 0.9rem;
        background: linear-gradient(135deg, var(--primary), var(--secondary));
        color: white;
        text-decoration: none;
        border-radius: 8px;
        text-align: center;
        display: block;
        font-weight: bold;
        transition: all 0.3s;
    }

    .btn-ver:hover {
        transform: scale(1.03);
        box-shadow: 0 10px 30px rgba(255, 0, 128, 0.5);
    }

    .empty-state {
        text-align: center;
        padding: 4rem 2rem;
        background: rgba(26, 31, 58, 0.4);
        border-radius: 15px;
        border: 2px dashed var(--text-muted);
    }

    .empty-state i {
        font-size: 5rem;
        color: var(--text-muted);
        margin-bottom: 1rem;
    }

    .empty-state h3 {
        color: var(--text-muted);
        font-size: 1.5rem;
        margin-bottom: 1rem;
    }

    @media (max-width: 768px) {
        .productos-grid {
            grid-template-columns: 1fr;
        }

        .filtros-container {
            justify-content: center;
        }
    }
</style>

<div class="page-header">
    <h1>🛒 Catálogo de Productos</h1>
    <p>Encuentra el componente perfecto para tu setup gamer</p>
</div>

<!-- Filtros -->
<div class="filtros-container">
    <a href="/productos" class="filtro-btn {{#unless categoriaSeleccionada}}active{{/unless}}">
        <i class="fas fa-th"></i> Todos
    </a>
    {{#each categorias}}
    <a href="/productos?categoria={{id}}" class="filtro-btn {{#if (eq ../categoriaSeleccionada id)}}active{{/if}}">
        <i class="{{icono}}"></i> {{nombre}}
    </a>
    {{/each}}
</div>

<div class="productos-info">
    Mostrando <strong>{{totalProductos}}</strong> productos
    {{#if categoriaSeleccionada}}
    en la categoría <strong>{{categoriaSeleccionada}}</strong>
    {{/if}}
</div>

{{#if productos}}
<div class="productos-grid">
    {{#each productos}}
    <article class="producto-card">
        {{#if (hasDiscount descuento)}}
        <div class="producto-badge">-{{descuento}}% OFF</div>
        {{/if}}
        
        <div class="producto-imagen">
            <i class="fas fa-microchip"></i>
        </div>
        
        <div class="producto-contenido">
            <span class="producto-categoria">{{categoria}}</span>
            <h3 class="producto-nombre">{{nombre}}</h3>
            <div class="producto-rating">{{stars rating}}</div>
            <p class="producto-descripcion">{{descripcion}}</p>
            
            <div class="producto-precio">
                {{#if (hasDiscount descuento)}}
                <div class="precio-original">{{formatPrice precio}}</div>
                <div class="precio-actual">{{formatPrice (calcDiscount precio descuento)}}</div>
                {{else}}
                <div class="precio-actual">{{formatPrice precio}}</div>
                {{/if}}
            </div>
            
            <div class="producto-stock">
                <i class="fas fa-check-circle"></i> {{stock}} unidades en stock
            </div>
            
            <a href="/producto/{{id}}" class="btn-ver">
                <i class="fas fa-eye"></i> Ver Detalles
            </a>
        </div>
    </article>
    {{/each}}
</div>
{{else}}
<div class="empty-state">
    <i class="fas fa-box-open"></i>
    <h3>No hay productos en esta categoría</h3>
    <p style="color: var(--text-muted); margin-top: 1rem;">Prueba con otra categoría o mira nuestro catálogo completo</p>
    <a href="/productos" class="filtro-btn" style="margin-top: 2rem; display: inline-block;">
        <i class="fas fa-arrow-left"></i> Ver Todos los Productos
    </a>
</div>
{{/if}}
