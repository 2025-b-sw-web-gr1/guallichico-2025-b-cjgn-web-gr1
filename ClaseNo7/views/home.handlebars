<style>
    .hero {
        background: linear-gradient(135deg, rgba(255, 0, 128, 0.2), rgba(0, 240, 255, 0.2));
        padding: 4rem 2rem;
        border-radius: 20px;
        text-align: center;
        margin-bottom: 3rem;
        border: 2px solid var(--primary);
        box-shadow: 0 10px 40px rgba(255, 0, 128, 0.3);
    }

    .hero h1 {
        font-size: 3.5rem;
        margin-bottom: 1rem;
        background: linear-gradient(135deg, var(--primary), var(--accent));
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        animation: glow 2s ease-in-out infinite alternate;
    }

    @keyframes glow {
        from { filter: drop-shadow(0 0 5px var(--primary)); }
        to { filter: drop-shadow(0 0 20px var(--accent)); }
    }

    .hero p {
        font-size: 1.3rem;
        color: var(--text-muted);
        margin-bottom: 2rem;
    }

    .hero-btn {
        display: inline-block;
        padding: 1rem 3rem;
        background: linear-gradient(135deg, var(--primary), var(--secondary));
        color: white;
        text-decoration: none;
        border-radius: 50px;
        font-weight: bold;
        font-size: 1.1rem;
        transition: all 0.3s;
        box-shadow: 0 5px 20px rgba(255, 0, 128, 0.4);
    }

    .hero-btn:hover {
        transform: translateY(-5px);
        box-shadow: 0 15px 40px rgba(255, 0, 128, 0.6);
    }

    .stats-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
        gap: 2rem;
        margin: 3rem 0;
    }

    .stat-card {
        background: linear-gradient(135deg, rgba(255, 0, 128, 0.1), rgba(121, 40, 202, 0.1));
        padding: 2rem;
        border-radius: 15px;
        text-align: center;
        border: 2px solid transparent;
        transition: all 0.3s;
    }

    .stat-card:hover {
        border-color: var(--accent);
        transform: translateY(-10px);
        box-shadow: 0 15px 40px rgba(0, 240, 255, 0.3);
    }

    .stat-icon {
        font-size: 3rem;
        margin-bottom: 1rem;
        background: linear-gradient(135deg, var(--primary), var(--accent));
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
    }

    .stat-number {
        font-size: 2.5rem;
        font-weight: bold;
        color: var(--accent);
    }

    .stat-label {
        color: var(--text-muted);
        margin-top: 0.5rem;
    }

    .section-title {
        font-size: 2.5rem;
        text-align: center;
        margin: 4rem 0 2rem;
        background: linear-gradient(135deg, var(--primary), var(--accent));
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
    }

    .productos-grid {
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
        gap: 2rem;
    }

    .producto-card {
        background: rgba(26, 31, 58, 0.6);
        border-radius: 15px;
        overflow: hidden;
        border: 2px solid transparent;
        transition: all 0.3s;
        position: relative;
    }

    .producto-card:hover {
        border-color: var(--primary);
        transform: translateY(-10px);
        box-shadow: 0 15px 40px rgba(255, 0, 128, 0.4);
    }

    .producto-badge {
        position: absolute;
        top: 1rem;
        right: 1rem;
        background: linear-gradient(135deg, var(--primary), var(--secondary));
        color: white;
        padding: 0.5rem 1rem;
        border-radius: 20px;
        font-size: 0.9rem;
        font-weight: bold;
        z-index: 10;
    }

    .producto-imagen {
        width: 100%;
        height: 250px;
        background: linear-gradient(135deg, var(--primary), var(--secondary));
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 5rem;
        color: white;
    }

    .producto-contenido {
        padding: 1.5rem;
    }

    .producto-categoria {
        display: inline-block;
        background: rgba(0, 240, 255, 0.2);
        color: var(--accent);
        padding: 0.3rem 0.8rem;
        border-radius: 15px;
        font-size: 0.85rem;
        margin-bottom: 0.5rem;
        border: 1px solid var(--accent);
    }

    .producto-nombre {
        font-size: 1.3rem;
        margin-bottom: 0.5rem;
        color: var(--text);
        font-weight: bold;
    }

    .producto-rating {
        color: var(--warning);
        margin-bottom: 1rem;
        font-size: 1.1rem;
    }

    .producto-descripcion {
        color: var(--text-muted);
        margin-bottom: 1rem;
        font-size: 0.95rem;
        line-height: 1.5;
    }

    .producto-precio {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 1rem;
    }

    .precio-original {
        text-decoration: line-through;
        color: var(--text-muted);
        font-size: 1rem;
    }

    .precio-actual {
        font-size: 2rem;
        font-weight: bold;
        background: linear-gradient(135deg, var(--primary), var(--accent));
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
    }

    .producto-stock {
        margin-bottom: 1rem;
        padding: 0.5rem;
        background: rgba(0, 255, 136, 0.1);
        border-radius: 8px;
        text-align: center;
        color: var(--success);
        font-size: 0.9rem;
    }

    .btn-ver {
        width: 100%;
        padding: 1rem;
        background: linear-gradient(135deg, var(--primary), var(--secondary));
        color: white;
        text-decoration: none;
        border-radius: 8px;
        text-align: center;
        display: block;
        font-weight: bold;
        transition: all 0.3s;
    }

    .btn-ver:hover {
        transform: scale(1.05);
        box-shadow: 0 10px 30px rgba(255, 0, 128, 0.5);
    }

    .categorias-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
        gap: 1.5rem;
        margin: 3rem 0;
    }

    .categoria-card {
        background: rgba(26, 31, 58, 0.6);
        padding: 2rem;
        border-radius: 15px;
        text-align: center;
        border: 2px solid transparent;
        transition: all 0.3s;
        text-decoration: none;
        color: var(--text);
    }

    .categoria-card:hover {
        border-color: var(--accent);
        transform: translateY(-10px);
        box-shadow: 0 15px 40px rgba(0, 240, 255, 0.3);
    }

    .categoria-icono {
        font-size: 3rem;
        margin-bottom: 1rem;
        background: linear-gradient(135deg, var(--primary), var(--accent));
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
    }

    .categoria-nombre {
        font-weight: bold;
        font-size: 1.1rem;
    }

    @media (max-width: 768px) {
        .hero h1 {
            font-size: 2rem;
        }

        .productos-grid {
            grid-template-columns: 1fr;
        }
    }
</style>

<!-- Hero Section -->
<section class="hero">
    <h1>🎮 Bienvenido a TechGamer Store</h1>
    <p>Los mejores componentes gaming al mejor precio</p>
    <a href="/productos" class="hero-btn">
        <i class="fas fa-shopping-bag"></i> Ver Catálogo Completo
    </a>
</section>

<!-- Stats -->
<div class="stats-grid">
    <div class="stat-card">
        <div class="stat-icon"><i class="fas fa-box-open"></i></div>
        <div class="stat-number">{{stats.productos}}+</div>
        <div class="stat-label">Productos Disponibles</div>
    </div>
    <div class="stat-card">
        <div class="stat-icon"><i class="fas fa-layer-group"></i></div>
        <div class="stat-number">{{stats.categorias}}</div>
        <div class="stat-label">Categorías</div>
    </div>
    <div class="stat-card">
        <div class="stat-icon"><i class="fas fa-shipping-fast"></i></div>
        <div class="stat-number">{{stats.envioGratis}}</div>
        <div class="stat-label">Envío Gratis</div>
    </div>
    <div class="stat-card">
        <div class="stat-icon"><i class="fas fa-shield-alt"></i></div>
        <div class="stat-number">{{stats.garantia}}</div>
        <div class="stat-label">Garantía Oficial</div>
    </div>
</div>

<!-- Categorías -->
<h2 class="section-title">🏷️ Categorías Populares</h2>
<div class="categorias-grid">
    {{#each categorias}}
    <a href="/productos?categoria={{id}}" class="categoria-card">
        <div class="categoria-icono">
            <i class="{{icono}}"></i>
        </div>
        <div class="categoria-nombre">{{nombre}}</div>
    </a>
    {{/each}}
</div>

<!-- Productos Destacados -->
<h2 class="section-title">⭐ Productos Destacados</h2>
<div class="productos-grid">
    {{#each productos}}
    <article class="producto-card">
        {{#if (hasDiscount descuento)}}
        <div class="producto-badge">-{{descuento}}% OFF</div>
        {{/if}}
        
        <div class="producto-imagen">
            <i class="fas fa-microchip"></i>
        </div>
        
        <div class="producto-contenido">
            <span class="producto-categoria">{{categoria}}</span>
            <h3 class="producto-nombre">{{nombre}}</h3>
            <div class="producto-rating">{{stars rating}}</div>
            <p class="producto-descripcion">{{descripcion}}</p>
            
            <div class="producto-precio">
                <div>
                    {{#if (hasDiscount descuento)}}
                    <div class="precio-original">{{formatPrice precio}}</div>
                    <div class="precio-actual">{{formatPrice (calcDiscount precio descuento)}}</div>
                    {{else}}
                    <div class="precio-actual">{{formatPrice precio}}</div>
                    {{/if}}
                </div>
            </div>
            
            <div class="producto-stock">
                <i class="fas fa-check-circle"></i> {{stock}} unidades disponibles
            </div>
            
            <a href="/producto/{{id}}" class="btn-ver">
                <i class="fas fa-eye"></i> Ver Detalles
            </a>
        </div>
    </article>
    {{/each}}
</div>
